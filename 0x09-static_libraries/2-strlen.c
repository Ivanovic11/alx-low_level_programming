#include <string.h>
#include "main.h"
#include <stdlib.h>
/**
  * _strlen - returns the lenght of a string
  *@s: the string to return it's lenght
  *Return: length of the string
  */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
