#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Putter[] = "\"Programming is like building a multilingual puzzle";

	puts(Putter);
	return (0);
}
